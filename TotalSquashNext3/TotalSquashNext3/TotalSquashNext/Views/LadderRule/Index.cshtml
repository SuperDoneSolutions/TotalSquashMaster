@model IEnumerable<TotalSquashNext.Models.LadderRule>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.challengeRange)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.challengeLower)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numLadders)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.challengeRange)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.challengeLower)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numLadders)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LadderRuleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LadderRuleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LadderRuleId })
        </td>
    </tr>
}

</table>
