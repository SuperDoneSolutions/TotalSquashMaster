@model IEnumerable<TotalSquashNext.Models.Building>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Organization.orgName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Organization.orgName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.buildingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.buildingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.buildingId })
        </td>
    </tr>
}

</table>
