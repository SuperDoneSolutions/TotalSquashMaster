@model IEnumerable<TotalSquashNext.Models.Booking>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.bookingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.userId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookingType.description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BookingRule.bookingRuleId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Court.courtDescription)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.bookingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.userId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookingType.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookingRule.bookingRuleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Court.courtDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.bookingNumber }) |
                @Html.ActionLink("Details", "Details", new { id=item.bookingNumber }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.bookingNumber })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
